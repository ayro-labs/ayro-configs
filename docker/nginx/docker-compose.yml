version: "3"
services:
  nginx:
    image: nginx
    container_name: nginx
    hostname: "ayro-nginx"
    volumes:
      - "<AYRO_HOME>/ayro-configs/nginx/default.conf:/etc/nginx/conf.d/default.conf"
      - "<AYRO_HOME>/ayro-configs/certificate:/etc/nginx/ssl"
    ports:
      - "80:80"
      - "443:443"
    networks:
      default:
        aliases:
          - "ayro-nginx"
networks:
  default:
    external:
      name: "ayro-network"